{
  "name": "inat-components",
  "license": "MIT",
  "scripts": {
    "boot": "lerna bootstrap",
    "build": "lerna run build",
    "generate": "cd apps/demo && yarn generate",
    "dev": "lerna exec --parallel -- yarn watch"
  },
  "devDependencies": {
    "lerna": "^5.1.6"
  }
}