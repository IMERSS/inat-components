{
  "name": "inat-components",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && yarn start",
    "bootstrap": "cd client && yarn install",
    "generate": "cd server && deno index.ts"
  }
}
