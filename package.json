{
  "name": "inat-components",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && yarn start",
    "server": "cd server && ts-node webserver.ts",
    "bootstrap": "cd client && yarn install",
    "generate": "cd server && ts-node index.ts"
  },
  "dependencies": {
    "date-fns": "^2.28.0"
  }
}
