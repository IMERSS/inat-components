{
  "name": "inat-components",
  "scripts": {
    "boot": "lerna bootstrap",
    "build": "lerna run build",
    "demo": "lerna run start --scope demo"
  },
  "devDependencies": {
    "lerna": "^5.1.6"
  }
}