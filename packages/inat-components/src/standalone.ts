import { InitStandalone } from "./types/standalone";
import TaxonPanel from "./components/taxon-panel/taxon-panel";

const initStandalone: InitStandalone = (props) => {
	// return (
	//
	// )
};

// @ts-ignore
window.inatComponents = {
	initStandalone
};
